# tests to check the parts of the whole optimization that includes
# optTopLevel (search in the same level of hybrids) and
# addHybridizationUpdate(net) that will add a new hybrid if needed, and deleteHybridizationUpdate()
